def primes():
    n = 2
    while True:
        if all(n % d != 0 for d in range(2, int(n**0.5) + 1)):
            yield n
        n += 1

# Пример использования
gen = primes()
for _ in range(10):
    print(next(gen))
